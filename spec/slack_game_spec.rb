require 'spec_helper'

describe SlackGame do
  it 'has a version number' do
    expect(SlackGame::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
